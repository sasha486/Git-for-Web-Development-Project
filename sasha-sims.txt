This is my codepen link:

https://codepen.io/sasha418/pen/QWGxJQO   


    1.What is Semantic HTML?
        Semantic HTML is markup that gives descriptive meaning for quick understanding and readiability.
    2. What is HTML used for? 
        HTML is used to give structure to a web page and it's content. 

    3. What is an attribute and where do we put it? 
        Attributes modify the characteristics of elements and go inside opening tags.

    4. What is the h1 tag used for? How many times should I use it on a page?
        h1 is the the largest heading tag used for titles and should only be used once on a page.

    5. Name two tags that have required attributes
        The <img> and <a> tags require attributes to work properly.

    6. What do we put in the head of our HTML document? 
        The head contains the title of the webpage and external links for styling and functioning. 

    7. What is an id? 
        An id is an attribute to be used to on an HTML element to be styled with CSS. The id can only be used on one element.

    8. What elements can I add an id to? 
        the id attribute can be used on any HTML element. 

    9. How many times can I use the same id on a page? 
        Only once; each id must be unique. 

    10. What is a class? 
        A class is used to selct elements to apply styling or other characteristics to. 
    11. What elements can I add a class to? 
        A class can be used on any HTML elements.

    12. How many times can I use the same class on a page? 
        The same class can be used multiple times on a page. 

    13. How do I get my link to open in a new tab?
        in the <a> tag, adding target="_blank" before the closing tag will open the link in a new tab. 

    14. What is the alt attribute in the image tag used for? 
        The alt attribute is used to display text as an alternative to the image if the image can't be viewed or loaded. 

    15. How do I reference an id?
        an id is referenced in CSS by #id

    16. What is the difference between a section and a div
        A sections groups content that is related and a div is used as a container more for styling purposes when a more semantic element can't be used. 

    17. What is CSS used for? 
        CSS is used to define the style, design, and layout of a webpage. 

    18. How to we select an element? Example - every h2 on the page
        Selecting elements in CSS is done by writing the element name in front of curly braces. For example, h2{
        }

    19. What is the difference between a class and an id? - Give me an example of when I might use each one
        Class is used when you have mulitple elements that have the same purpose and type such as adding the same style to rows in a table or borders to images.
        Id is used for a single element with it's own unique purpose such as a title element. 

    20. How do we select classes in CSS?
        Classes are selected with a period (.className)

    21. How do we select a p element with a single class of “human””?
        <p class = "human"> in the HTML and .human{} in the CSS.

    22. What is a parent child selector? When would this be useful? 
        Parent child selector selects all elements that are a direct child of the element. This could be useful when styling all of the paragraphs inside of a div for example. div would be the parent and the p would be the child. div>p 

    23. How do you select all links within a div with the class of sidebar?
        div.sidebar a:link{}

    24. What is a pseudo selector?
        It specifies how the state of the element will change. 

    25. What do we use the change the spacing between lines?
        In CSS, the line-height property changes spacing between lines.  

    26. What do we use to change the spacing between letters?
        letter-spacing:

    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?
        text-transform: uppercase, text-transform: lowercase, text-transform: capitalize

    28. How do I add a 1px border around my div that is dotted and black?
        div{
            border: 1px dotted #000000;

        }
    29. How do I select everything on the page? 
        *
    30. How do I write a comment in CSS?
        /* comment */

    31. How do I find out what file I am in, when I am using the command line? 
        pwd

    32. Using the command line - how do I see a list of files/folders in my current folder?
        ls

    33. How do I remove a file via the command line? Why do I have to be careful with this? 
        rm file name. This cannot be undone.

    34. Why should I use version control? 
        It keeps track of all changes so every member can work on the latest version and collaborate effectively.

    35. How often should I commit to github?
        Every 20 minutes

    36. What is the command we would use to push our repo up to github? 
        git push

    37. Walk me through Lambda's git flow. 
        fork repository, clone the forked link, open file for the folder in the terminal, git clone paste link, 
        enter the directory, create a branch with your first and last name, make changes and add files, commit changes, 
        push to submit, add collaborator, submit pull request, compare and pull request with your main branch selected. 

Stretch Questions

    1. What is the difference between an inline element and a block element?

        block elements take up the entire width available and always start on a new line. Inline elements don't have top and bottom margins, but block elements do. 
        
    2. What happens when an element is positioned absolutely? 
        
        Absolute position moves an elment relative to the nearest positioned ancestor.

    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? 

    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default

        display block: div, header, section
        display inline: span, buttons
        inline-block: img
    5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them?

    The box model is a default set of rules for CSS that sizes elements according to margin, padding, and borders around content. The fix for this is to reset the box-sizing in css to border-box. 

